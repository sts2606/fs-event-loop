import os from 'node:os';

// if (os.EOL === '\n') {
//   console.log(
//     'OS uses LF as the end-of-line marker. Linux and macOS use LF as the end-of-line marker.',
//   );
// } else if (os.EOL === '\r\n') {
//   console.log(
//     'OS uses CRLF as the end-of-line marker. Windows uses CRLF as the end-of-line marker.',
//   );
// } else {
//   console.log('Unknown end-of-line marker:', JSON.stringify(os.EOL));
// }

// console.log(`Платформа: ${os.platform()}`); // Показує тип операційної системи
// console.log(`Версія ОС: ${os.release()}`); // Виводить версію операційної системи
// console.log(`Архітектура процесора: ${os.arch()}`); // Повідомляє про архітектуру CPU
// console.log(`Інформація про CPU:`, os.cpus(), os.cpus().length); // Дає деталі процесорів
// console.log(`Загальна пам'ять: ${os.totalmem()} байт`); // Загальний об'єм оперативної пам'яті
// console.log(`Вільна пам'ять: ${os.freemem()} байт`); // Вільний об'єм оперативної пам'яті
// console.log(`Тимчасова директорія: ${os.tmpdir()}`); // Шлях до тимчасової папки
// console.log(`Мережеві інтерфейси:`, os.networkInterfaces());
